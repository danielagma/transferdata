
@workspace Please analyze the test files inside src/tests/functional/rfq-flow/rfq-window/. I need to find the specific .spec.ts files that inject RFQs using the RabbitMQ implementation (referencing utilities from src/utils/rabbitmq.ts or src/factories/rabbitmq.factory.ts).

Please output:

The exact file path of a robust test spec in that folder that raises a Switch or Butterfly RFQ via RMQ.

A brief code snippet from that file showing exactly how the RMQ helper function is called to inject the RFQ payload.
