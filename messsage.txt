21027102 y 21027103 · el trade 8

Event.Type: "TradeEvent" and "21027102"
Event.Type: "TradeEvent" and "21027103"

Buscas ShortNote8 en el XML. Cierra el AC 4 de DWU-538.

La regla de la 538 es: un trade de TOMS con TransactionType en RF/AE/MT/TT y ShortNote8 poblado hay que descartarlo, porque significa que ese trade salió de Darwin y vuelve. Los AC 1 a 4 son esos cuatro casos positivos.

Hoy están dados por intestables: Murat escribió en el ticket que la función de STP Hub que puebla el ShortNote8 no existe en UAT, y por eso los verificó con tests de componente en local.

*Pero el trade 8 es TT, que es justo el AC 4, y Sergio escribió sobre él "this one should be filtered".* Si su ShortNote8 viene poblado, el caso positivo lo tenemos y la premisa de que no se puede probar se cae.

Los otros seis

Event.Type: "TradeEvent" and "21027094"
Event.Type: "TradeEvent" and "21027095"
Event.Type: "TradeEvent" and "21027096"
Event.Type: "TradeEvent" and "21027097"
Event.Type: "TradeEvent" and "21027098"
Event.Type: "TradeEvent" and "21027099"

ShortNote8, esperado vacío. Cierra los AC 5 a 9 de DWU-538.

Son los casos negativos: no filtrar cuando ShortNote8 está vacío. Los siete publicaron, o sea que no se filtraron. Pero eso solo no prueba nada: si alguno hubiera venido con ShortNote8 poblado y aun así publicó, el caso no es el que creemos, es un fallo. Hay que ver el campo vacío para poder firmar.

IsInternalMirror, en los mismos XML. Cierra InternalTradeType en DWU-418.

La regla del ticket es literal: "If IsInternalMirror is true, set the field to Main, otherwise set the value to Mirror". Nuestros payloads publican Mirror, que es la rama del false.

Sin leer la entrada estaría firmando la salida a ciegas. Es la única fila de la 418 que me falta para poder cerrarla hoy. Si el campo no está en el XML porque se construye dentro de Darwin:

"IsInternalMirror" and "21027094"

Lo que queda firmado si sale todo

DWU-416, DWU-417 y DWU-418 completas con los siete trades, y DWU-538 con sus negativos probados y, si hay suerte, un positivo real.
